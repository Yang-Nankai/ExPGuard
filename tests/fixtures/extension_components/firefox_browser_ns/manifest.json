{
  "manifest_version": 2,
  "name": "Firefox browser.* Namespace Demo",
  "version": "1.0",
  "description": "Firefox WebExtension: a cookie read via the browser.* namespace is POSTed in a fetch body — must be a DATA_LEAK, proving browser.* is modeled like chrome.*.",
  "permissions": ["cookies", "<all_urls>"],
  "background": {
    "scripts": ["background.js"]
  },
  "applications": {
    "gecko": {
      "id": "{11111111-1111-1111-1111-111111111111}",
      "strict_min_version": "91.0"
    }
  }
}
