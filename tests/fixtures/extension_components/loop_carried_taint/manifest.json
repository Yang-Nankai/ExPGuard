{
  "manifest_version": 3,
  "name": "Loop Carried Taint Demo",
  "version": "1.0",
  "description": "Taint only reaches the sink variable on the second loop iteration, which requires CFG back edges plus a bounded fixpoint.",
  "permissions": ["bookmarks"],
  "externally_connectable": { "matches": ["*://*/*"] },
  "background": { "service_worker": "background.js" }
}
