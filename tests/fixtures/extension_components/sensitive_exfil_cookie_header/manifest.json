{
  "manifest_version": 3,
  "name": "Sensitive Exfil Cookie Header Demo",
  "version": "1.0",
  "description": "A cookie is read and sent as the Cookie request header — benign auth, must NOT be a DATA_LEAK.",
  "permissions": ["cookies"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
