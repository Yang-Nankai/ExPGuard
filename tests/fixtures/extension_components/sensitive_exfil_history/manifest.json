{
  "manifest_version": 3,
  "name": "Sensitive Exfil History Demo",
  "version": "1.0",
  "description": "chrome.history.search results are POSTed to a remote server in the request body — sensitive-data exfiltration.",
  "permissions": ["history"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
