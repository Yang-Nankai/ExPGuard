{
  "manifest_version": 3,
  "name": "Sensitive Exfil Axios Header Demo",
  "version": "1.0",
  "description": "A cookie is placed into an axios request header — benign auth. Header suppression must work for axios as well as fetch / XHR.",
  "permissions": ["cookies"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
