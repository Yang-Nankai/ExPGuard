{
  "manifest_version": 3,
  "name": "Consumed Storage Demo",
  "version": "1.0",
  "description": "Attacker-controlled data is written to a chrome.storage key that another component reads back.",
  "permissions": ["storage", "tabs"],
  "externally_connectable": { "matches": ["*://*/*"] },
  "background": { "service_worker": "background.js" }
}
