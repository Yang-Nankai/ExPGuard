{
  "manifest_version": 3,
  "name": "DNR Rule Hijack Demo",
  "version": "1.0",
  "description": "An external message drives chrome.declarativeNetRequest.updateDynamicRules — attacker-controlled request redirect (privilege escalation).",
  "permissions": ["declarativeNetRequest"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
