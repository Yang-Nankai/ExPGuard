{
  "manifest_version": 3,
  "name": "Sensitive Exfil XHR Header Demo",
  "version": "1.0",
  "description": "A cookie is sent via XMLHttpRequest.setRequestHeader — benign auth, must NOT be a DATA_LEAK (cross-API suppression).",
  "permissions": ["cookies"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
