{
  "manifest_version": 3,
  "name": "Offscreen Relay Demo",
  "version": "1.0",
  "description": "Background creates an offscreen document; offscreen reads window message and forwards user text to chrome.bookmarks.create via background.",
  "permissions": ["offscreen", "bookmarks"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
