{
  "manifest_version": 3,
  "name": "Sensitive Exfil XHR Body Demo",
  "version": "1.0",
  "description": "A cookie is exfiltrated through XMLHttpRequest.send(body). Unlike the header variant, a body leak IS reported as DATA_LEAK.",
  "permissions": ["cookies"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
