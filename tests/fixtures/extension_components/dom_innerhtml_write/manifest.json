{
  "manifest_version": 3,
  "name": "innerHTML Write Sink Demo",
  "version": "1.0",
  "description": "Popup writes tainted document.URL directly into element.innerHTML — raw DOM XSS via member assignment.",
  "background": {
    "service_worker": "background.js",
    "type": "module"
  },
  "action": {
    "default_popup": "popup.html"
  }
}
