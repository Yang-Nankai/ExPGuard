{
  "manifest_version": 3,
  "name": "Sensitive Exfil System CPU Demo",
  "version": "1.0",
  "description": "Hardware fingerprint from chrome.system.cpu.getInfo is POSTed to a third party — DATA_LEAK with sourceRemark 'system.cpu'.",
  "permissions": ["system.cpu"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
