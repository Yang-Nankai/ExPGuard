{
  "manifest_version": 3,
  "name": "Callback Entrypoint Sweep Demo",
  "version": "1.0",
  "description": "A sink lives in a function only ever handed to an unmodeled API, so it is reachable only via the entry-point sweep.",
  "permissions": ["bookmarks"],
  "content_scripts": [{ "matches": ["<all_urls>"], "js": ["content.js"] }],
  "background": { "service_worker": "background.js" }
}
