{
  "manifest_version": 3,
  "name": "Sensitive Exfil Cookie URL Demo",
  "version": "1.0",
  "description": "A cookie is appended to the request URL (query string) — exfiltration via the URL, not the body. Must still be reported as DATA_LEAK.",
  "permissions": ["cookies"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
