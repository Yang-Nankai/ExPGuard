{
  "manifest_version": 3,
  "name": "Sensitive Exfil Identity Token Demo",
  "version": "1.0",
  "description": "An OAuth token from chrome.identity.getAuthToken is POSTed to a third party — DATA_LEAK with sourceRemark 'identity.authToken'.",
  "permissions": ["identity"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
