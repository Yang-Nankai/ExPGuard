{
  "manifest_version": 3,
  "name": "Sensitive Exfil Axios Body Demo",
  "version": "1.0",
  "description": "A cookie is POSTed via axios as the request data — exfiltration must be reported as DATA_LEAK across the axios API too (not just fetch).",
  "permissions": ["cookies"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
