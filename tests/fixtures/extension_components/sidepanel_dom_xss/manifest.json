{
  "manifest_version": 3,
  "name": "Side Panel DOM XSS Demo",
  "version": "1.0",
  "description": "Side panel reads document.URL and writes it back into innerHTML — a classic DOM XSS in extension context.",
  "permissions": ["sidePanel"],
  "side_panel": {
    "default_path": "sidepanel.html"
  },
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
