{
  "manifest_version": 3,
  "name": "New Tab Override Demo",
  "version": "1.0",
  "description": "Overrides newtab; the page reads window.name and writes it into innerHTML.",
  "chrome_url_overrides": {
    "newtab": "newtab.html"
  }
}
