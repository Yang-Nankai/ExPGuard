{
  "manifest_version": 3,
  "name": "Inline Script Popup Demo",
  "version": "1.0",
  "description": "Popup ships its logic as an inline <script> block; the analyzer should materialize it via the .epg-inline virtual file.",
  "permissions": ["bookmarks"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  },
  "action": {
    "default_popup": "popup.html"
  }
}
