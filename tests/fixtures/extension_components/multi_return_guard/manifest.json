{
  "manifest_version": 3,
  "name": "Multi Return Guard Demo",
  "version": "1.0",
  "description": "A guard helper with several return statements must not constant-fold to its last one and prune the caller.",
  "permissions": ["bookmarks"],
  "externally_connectable": { "matches": ["*://*/*"] },
  "background": { "service_worker": "background.js" }
}
