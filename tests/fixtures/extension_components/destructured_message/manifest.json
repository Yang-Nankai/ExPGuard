{
  "manifest_version": 3,
  "name": "Destructured Message Demo",
  "version": "1.0",
  "description": "An external message payload is destructured before reaching a privileged sink. Destructuring an opaque tainted value must not drop the taint.",
  "permissions": ["bookmarks"],
  "externally_connectable": { "matches": ["*://*/*"] },
  "background": { "service_worker": "background.js" }
}
