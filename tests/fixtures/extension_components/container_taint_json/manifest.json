{
  "manifest_version": 3,
  "name": "Container Taint JSON Demo",
  "version": "1.0",
  "description": "A privileged sink reads a field from a JSON.parse(taintedString) result — taint must flow through the container.",
  "permissions": ["bookmarks"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  },
  "action": {
    "default_popup": "popup.html"
  }
}
