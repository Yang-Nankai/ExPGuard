{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -E '^.*/src/.*\\.ts$' >/dev/null && (cd \"$CLAUDE_PROJECT_DIR\" && npx tsc --noEmit 2>&1 | tail -40) || true"
          }
        ]
      }
    ]
  }
}
